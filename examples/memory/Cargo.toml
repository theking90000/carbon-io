[package]
name = "carbon-example-memory"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false

[workspace]

[dependencies]
carbon-io = { path = "../.." }
futures = "0.3.32"

[lints.rust]
unsafe_code = "forbid"
